spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/dtbSyncLog_Hmg
    username: postgres
    password: "BRU*980192@no"
    hikari:
      maximum-pool-size: 5
  jackson:
    time-zone: America/Sao_Paulo
    serialization:
      WRITE_DATES_AS_TIMESTAMPS: false

# Propriedades da aplicação (usadas por AppProperties)
app:
  dtm:
    # Nome da VIEW que retorna as DTMs pendentes
    view: vw_dtm_pedidocoleta_unica
    # Tabela de lock/idempotência
    lockTable: dtm_automation_lock
  salvarColeta:
    # Base da API que expõe /api/v1/PedidoColeta/SalvaColeta
    baseUrl: http://localhost:5000
    # GUID do usuário (TokenHash)
    tokenHash: "00000000-0000-0000-0000-000000000000"
    timeoutSeconds: 30

logging:
  level:
    root: INFO
    com.tecnolog.autocoleta: DEBUG

feign:
  client:
    config:
      salvarColeta:
        connectTimeout: 30000
        readTimeout: 30000

